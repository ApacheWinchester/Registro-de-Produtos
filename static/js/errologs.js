{% load static %}

function errorlogin() {
        const login = document.getElementById('ipt-login').value;
        if (!email) {
              document.getElementById('erro-aviso').style.display = "block";
        }
}